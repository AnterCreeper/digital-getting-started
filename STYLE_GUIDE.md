# PPT 与讲义格式规范

本规范用于保证课程内容长期演进时风格稳定、结构一致、可自动化发布。

## 1. 目录与命名规范

每讲必须放在：

- `chapters/<chapter-slug>/<lecture-slug>/`

其中必须存在：

- `slides.tex`
- `handout.tex`
- `metadata.json`
- `figures/`

命名规则：

- `chapter-slug`: `chNN-topic-name`
- `lecture-slug`: `lNN-topic-name`
- 图文件：`snake_case`（例如 `fsm_state_transition.pdf`）

## 2. metadata.json 规范（必填）

```json
{
  "chapter_title": "章节名",
  "title": "讲次标题",
  "subtitle": "副标题",
  "summary": "一句话摘要",
  "keywords": ["关键词1", "关键词2"],
  "difficulty": "本科基础",
  "duration": "90 min",
  "updated": "YYYY-MM-DD"
}
```

说明：
- `keywords` 用于站点检索和筛选
- `updated` 用于站点卡片显示

## 3. PPT 结构规范

推荐页序（最少 6 页）：

1. 标题页
2. 学习目标
3. 核心概念/方法
4. 例题或图示解释
5. 常见误区/边界条件
6. 小结与课后任务

约束：
- 不要单讲私自更换全局主题/字体/色板
- 代码块样式沿用 `styles/beamer-style.tex`
- 一页一主点，长段文本拆分为图表或分点

## 4. 讲义结构规范

推荐节序：

1. Learning Objectives
2. Core Concepts
3. Key Figure / Table
4. In-Class Checklist
5. After-Class Practice

约束：
- 讲义术语必须与PPT一致
- 至少包含一个可执行练习任务
- `tipbox`/`warnbox` 用于提示和风险

### 4.0 教学组织原则

在内容层面，本项目不是把若干知识点排成目录，而是要把读者带上一条可反复回来的学习路径。

优先采用以下组织思路：

- **先立坐标，再讲细节**。尤其是导论章，不要一上来就堆定义或术语，要先回答“这章为什么存在”“这一章和后续章节是什么关系”。
- **主线必须单一**。一讲可以包含历史、对象、流程、案例，但只能有一条主干叙事线，其他内容都应服务于主线，而不是并列抢权重。
- **事实服务于思想，不要倒果为因**。历史、公司、案例、图示的作用，是支撑主干判断，不是把正文写成资料汇编。
- **对象优先于观点**。尽量让读者感觉是在接近“对象本身的结构”，而不是在接受作者的一套宏观立场。
- **关系优先于名词**。讲清“这是什么、为什么重要、和前后对象有什么关系”，比堆更多术语更重要。

### 4.0a 螺旋式学习

本项目默认采用“螺旋式学习”而不是“一次讲透”。

这意味着：

- 第一遍先帮助读者建立轮廓、位置感和最小直觉
- 后续章节再反复回到同一概念，逐步增加技术细节和精度
- 不要求在第一次出现时把所有细节压满
- 但第一次出现时必须把“它为什么存在、后面会在哪里回来”讲清楚

推荐写法：

- 用“这里先抓住轮廓，后面会回来加深”这样的提示，帮助自学者降低焦虑
- 在后文再次点亮前文概念，形成回钩和闭环
- 让同一个概念在不同章节里承担不同深度，而不是一次性解释完后再也不提

### 4.1 写作口吻

本项目面向本科生，正文要做到“可读、可信、可继续往下学”，不要写成冷冰冰的说明书，也不要写成过度抒情的序言。

**核心原则**：问题导向的口语化叙述优先。好的技术写作不是“念名词”，而是“带着问题走”——先让学生感受到“这个问题确实存在，而且值得搞清楚”，再给出概念和结论。

建议风格：

- **开头先给问题，再给定义**。用具体的困惑、场景或矛盾引出概念，比直接下定义更有效。
- **解释优先于堆砌术语**。术语是工具，不是目标。
- **保留自然的带读感**。适度的口语化、设问、场景化叙述可以降低认知门槛，但需与内容密度平衡。
- **句子尽量清楚、直接、少绕弯**。
- **每节末尾最好有一个能把读者拉回现实系统的问题或小结**。
- **把重点放在对象、关系和原因上**。优先回答“这是什么、为什么会这样、它和前后对象有什么关系”。
- **少写作者自己的“全局视角”总结**，尽量贴着学生当下正在理解的问题展开。
- **允许使用“你”，但不要带压迫感**。第二人称可以帮助建立陪读感，但不要写成命令式训话。
- **不要过早抢结论**。先让读者看到问题、感到困惑，再给更高层的组织框架，通常比直接宣布“正确理解”更有教学推动力。
- **一段文字最好只完成一个认知动作**：提出问题、建立直觉、给定义、讲边界、做回钩，尽量不要在一个长段里全做完。

**关于人称的使用**：

- **鼓励**：自然的叙述视角（如“我们先看看…”“这里有一个值得注意的点…”）、场景化设问（如“为什么手机主板上有那么多小方块？”）。
- **避免**：命令式、说教式的第二人称压迫感（如“你必须记住”“你应该明白”“你会发现”）。这不是禁止“你”字，而是禁止居高临下的口吻。
- **倾向**：中性的说明式语气（“设计者需要…”“在这种情况下…”“结论是…”），但在导论和过渡段允许适度的第一人称复数或自然叙述。

**关于句子密度**：

- 可以高密度，但不要高压。
- 长句要有明显层次，尽量避免连续几句都在做结论宣告。
- 当一个判断比较抽象时，优先补一个场景、类比或对象例子，再给结论。
- 段首句尽量承担“带读者跨门槛”的任务，而不是直接变成总结句。

### 4.2 `tipbox` / `warnbox` 使用原则

这两个环境不是用来做课件式“重点标红”，而是用来帮助自学者完成认知转弯。

- `tipbox`：用于建立直觉、给观察方法、降低阅读焦虑、提示“后面会回来加深”
- `warnbox`：用于指出常见误解、抽象失效条件、边界条件和容易混淆之处

推荐：

- 短、准、像陪读者说一句关键提醒
- 能和正文形成互补，而不是重复正文结论

避免：

- 写成口号
- 写成课堂重点提示框
- 写成作者训诫

### 4.3 一讲完成后的自查问题

在提交前，除了编译通过，还应至少自问以下问题：

1. 这讲有没有一条单一主线？
2. 历史、案例、公司、图示是不是都在服务主线，而不是并列抢位置？
3. 读者读完后，脑中有没有新增一个可复用的判断框架？
4. 这一讲是否给了读者足够的“位置感”：它和前后章节是什么关系？
5. 有没有明显的说教式句子需要收掉？
6. 有没有某些段落只在堆名词，却没有讲关系？
7. 有没有给出“这一遍先认识轮廓，后面会回来加深”的螺旋式提示？

### 4.4 导论章特别提醒

对导论章（例如第 1 章）尤其要注意：

- 不要写成行业资料汇编
- 不要让历史线喧宾夺主
- 不要让价值判断代替对象分析
- 不要在一章里并列塞进多条主线
- 要先建立后续学习的认知地图，再进入对象、流程和产业环境

不建议的写法：

- 过多宏大叙事
- 过多修辞堆叠
- 过度学术化的长句
- 说教味过重、命令式第二人称压迫感太强
- 作者视角过强、像在对编辑或课程设计者解释文章结构
- 只讲名词，不讲关系
- 只讲结论，不讲为什么

## 5. 图与资产规范

- 原始图和导出图都放在讲次 `figures/`
- 优先矢量图（`.pdf`、`.svg`）
- 若使用位图，优先 `png` 或 `jpg`
- `webp` 不应直接作为 LaTeX 主素材；若来源是 `webp`，应先转换为 `png`
- 外部图必须在讲义或备注中给来源说明

## 6. 构建与发布规范

提交前最少执行：

```bash
make check-style
make all
make site
```

编译约束：

- 本仓库当前中文主路线为 `xelatex`
- 若讲次包含中文标题、正文或注释，不要假设 `pdflatex` 可正常工作

输出要求：
- `pdf/<chapter>_<lecture>_slides.pdf`
- `pdf/<chapter>_<lecture>_handout.pdf`
- `site/assets/lectures/<chapter>_<lecture>/...`

## 7. 允许改动边界

- 内容改动：`chapters/`
- 风格改动：`styles/`
- 自动化改动：`scripts/`
- 部署改动：`docs/STATIC_SITE_DEPLOY.md` 与 `scripts/build_site.py`

不要把全部改动混在一次提交里，除非明确是全局升级任务。
